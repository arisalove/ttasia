diff --git a/middleware.ts b/middleware.ts
index a251299..3543258 100644
--- a/middleware.ts
+++ b/middleware.ts
@@ -1,4 +1,5 @@
 import { NextResponse, type NextRequest } from 'next/server';
+import type { CookieOptions } from '@supabase/ssr';
 import { DEMO_SESSION_COOKIE, ROLE_COOKIE } from './src/lib/auth/cookies';
 
 /**
@@ -50,7 +51,7 @@ export async function middleware(request: NextRequest) {
       {
         cookies: {
           getAll: () => request.cookies.getAll(),
-          setAll: (cookiesToSet) => {
+          setAll: (cookiesToSet: { name: string; value: string; options: CookieOptions }[]) => {
             cookiesToSet.forEach(({ name, value }) => request.cookies.set(name, value));
             response = NextResponse.next({ request });
             cookiesToSet.forEach(({ name, value, options }) => response.cookies.set(name, value, options));
diff --git a/src/app/api/auth/signup/buyer/route.ts b/src/app/api/auth/signup/buyer/route.ts
index 4d9c559..b6615bc 100644
--- a/src/app/api/auth/signup/buyer/route.ts
+++ b/src/app/api/auth/signup/buyer/route.ts
@@ -34,7 +34,7 @@ export async function POST(request: Request) {
   }
   const userId = data.user.id;
   await supabase.from('users').insert({ id: userId, email: parsed.data.email, full_name: parsed.data.fullName, phone: parsed.data.phone, role: 'buyer' });
-  const { data: biz } = await supabase
+  const { data: biz, error: bizError } = await supabase
     .from('business_profiles')
     .insert({
       owner_user_id: userId,
@@ -47,6 +47,9 @@ export async function POST(request: Request) {
     })
     .select('id')
     .single();
+  if (bizError || !biz) {
+    return NextResponse.json({ error: bizError?.message ?? 'Could not create business profile.' }, { status: 500 });
+  }
   await supabase.from('buyer_profiles').insert({ user_id: userId, business_profile_id: biz.id, business_type: parsed.data.businessType });
 
   return NextResponse.json({ ok: true, redirectTo: '/buyer/dashboard' });
diff --git a/src/app/api/auth/signup/supplier/route.ts b/src/app/api/auth/signup/supplier/route.ts
index 2346d95..d331cbc 100644
--- a/src/app/api/auth/signup/supplier/route.ts
+++ b/src/app/api/auth/signup/supplier/route.ts
@@ -34,7 +34,7 @@ export async function POST(request: Request) {
   }
   const userId = data.user.id;
   await supabase.from('users').insert({ id: userId, email: parsed.data.email, full_name: parsed.data.fullName, phone: parsed.data.phone, role: 'supplier' });
-  const { data: biz } = await supabase
+  const { data: biz, error: bizError } = await supabase
     .from('business_profiles')
     .insert({
       owner_user_id: userId,
@@ -47,6 +47,9 @@ export async function POST(request: Request) {
     })
     .select('id')
     .single();
+  if (bizError || !biz) {
+    return NextResponse.json({ error: bizError?.message ?? 'Could not create business profile.' }, { status: 500 });
+  }
   const slug = parsed.data.storeName
     .toLowerCase()
     .replace(/[^a-z0-9]+/g, '-')
diff --git a/src/app/supplier/orders/page.tsx b/src/app/supplier/orders/page.tsx
index 8e8ebe8..7e5e17a 100644
--- a/src/app/supplier/orders/page.tsx
+++ b/src/app/supplier/orders/page.tsx
@@ -1,4 +1,3 @@
-import Link from 'next/link';
 import { OrderListItem } from '@/components/order/order-list-item';
 import { EmptyState } from '@/components/ui/empty-state';
 import { getSession } from '@/lib/auth/session';
diff --git a/src/components/messages/messenger.tsx b/src/components/messages/messenger.tsx
index 1edc867..f508eba 100644
--- a/src/components/messages/messenger.tsx
+++ b/src/components/messages/messenger.tsx
@@ -45,8 +45,9 @@ export function Messenger({
 
   // Deep-link: resume/create a thread with a given counterparty then select it.
   useEffect(() => {
-    const counterpartyId = counterpartyParam ? searchParams.get(counterpartyParam) : null;
-    if (!counterpartyId || !startThreadHref) return;
+    if (!counterpartyParam || !startThreadHref) return;
+    const counterpartyId = searchParams.get(counterpartyParam);
+    if (!counterpartyId) return;
     const existing = threads.find((t) => t.counterpartyId === counterpartyId);
     if (existing) {
       setActiveThreadId(existing.id);
diff --git a/src/components/product/product-card.tsx b/src/components/product/product-card.tsx
index 2201d71..12fcea2 100644
--- a/src/components/product/product-card.tsx
+++ b/src/components/product/product-card.tsx
@@ -19,7 +19,7 @@ export function ProductCard({
   isFavourite,
 }: {
   product: Product;
-  supplier?: Pick<SupplierProfile, 'storeName' | 'storeSlug' | 'district'> & { district?: string };
+  supplier?: Pick<SupplierProfile, 'storeName' | 'storeSlug'>;
   isBuyer: boolean;
   isFavourite?: boolean;
 }) {
diff --git a/src/lib/data/suppliers.ts b/src/lib/data/suppliers.ts
index 4ba23b7..eba02a0 100644
--- a/src/lib/data/suppliers.ts
+++ b/src/lib/data/suppliers.ts
@@ -205,7 +205,8 @@ export async function upsertProduct(input: UpsertProductInput): Promise<Product>
   if (productId) {
     await supabase.from('products').update(payload).eq('id', productId);
   } else {
-    const { data } = await supabase.from('products').insert(payload).select('id').single();
+    const { data, error } = await supabase.from('products').insert(payload).select('id').single();
+    if (error || !data) throw new Error(error?.message ?? 'Could not create product.');
     productId = data.id;
   }
   if (input.imageUrl) {
diff --git a/src/lib/domain/types.ts b/src/lib/domain/types.ts
index da7b821..e763a88 100644
--- a/src/lib/domain/types.ts
+++ b/src/lib/domain/types.ts
@@ -11,19 +11,7 @@ export type Locale = 'en' | 'ms';
 
 export type UserRole = 'buyer' | 'supplier' | 'admin';
 
-export type SabahDistrict =
-  | 'Tawau'
-  | 'Kota Kinabalu'
-  | 'Sandakan'
-  | 'Lahad Datu'
-  | 'Semporna'
-  | 'Kunak'
-  | 'Keningau'
-  | 'Kota Belud'
-  | 'Penampang'
-  | 'Putatan';
-
-export const SABAH_DISTRICTS: SabahDistrict[] = [
+export const SABAH_DISTRICTS = [
   'Tawau',
   'Kota Kinabalu',
   'Sandakan',
@@ -34,7 +22,9 @@ export const SABAH_DISTRICTS: SabahDistrict[] = [
   'Kota Belud',
   'Penampang',
   'Putatan',
-];
+] as const;
+
+export type SabahDistrict = (typeof SABAH_DISTRICTS)[number];
 
 export type VerificationStatus = 'unverified' | 'pending' | 'verified' | 'rejected' | 'suspended';
 
diff --git a/src/lib/domain/validation.ts b/src/lib/domain/validation.ts
index 54c4d9c..5103d20 100644
--- a/src/lib/domain/validation.ts
+++ b/src/lib/domain/validation.ts
@@ -1,7 +1,7 @@
 import { z } from 'zod';
 import { SABAH_DISTRICTS } from './types';
 
-const districtEnum = z.enum(SABAH_DISTRICTS as [string, ...string[]]);
+const districtEnum = z.enum(SABAH_DISTRICTS);
 
 export const loginSchema = z.object({
   email: z.string().min(1, 'Email is required').email('Enter a valid email address'),
diff --git a/src/lib/supabase/server.ts b/src/lib/supabase/server.ts
index dddea48..5c7b8d3 100644
--- a/src/lib/supabase/server.ts
+++ b/src/lib/supabase/server.ts
@@ -1,4 +1,4 @@
-import { createServerClient } from '@supabase/ssr';
+import { createServerClient, type CookieOptions } from '@supabase/ssr';
 import { cookies } from 'next/headers';
 import { isSupabaseConfigured } from './env';
 
@@ -19,7 +19,7 @@ export async function createClient() {
       getAll() {
         return cookieStore.getAll();
       },
-      setAll(cookiesToSet) {
+      setAll(cookiesToSet: { name: string; value: string; options: CookieOptions }[]) {
         try {
           cookiesToSet.forEach(({ name, value, options }) => cookieStore.set(name, value, options));
         } catch {
